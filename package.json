{
  "name": "avatarer",
  "version": "0.1.0",
  "private": true,
  "description": "Seeded p5.js full-body avatar rig with manual and MediaPipe pose drivers.",
  "scripts": {
    "start": "python3 -m http.server 5173 --bind 0.0.0.0",
    "check": "node --check src/sketch.js"
  }
}
